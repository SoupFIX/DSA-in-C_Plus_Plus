#include <iostream>
#include <stack>
using namespace std;
string reverse(string s)
{
    string s1, s2, s3;
    int i = 0;
    int word_count = 0;
    while (s[i] != " ")
    {
        s1[i] += s[i];
    }
    word_count++;
    while()
}
int main()
{
    string s;
    cout << "Enter the string: ";
    getline(cin, s);
    reverse(s);
}